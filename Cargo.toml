[package]
name = "bevy-test"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["jpeg", "mp3"]}
rand = "*"

[profile.dev.package."*"]
opt-level = 3

[features]
dev = ["bevy/dynamic_linking"]